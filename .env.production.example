# Copy to .env.production and set the public Worker URL.
# Vite auto-loads .env.production during vite build, so the GitHub Pages build
# picks up the public API with NO workflow changes. This is a public URL, not a secret.
VITE_API_URL=https://maak.<your-sub>.workers.dev
